*	Now we can draw the grid with an offset.
	This will be useful when drawing the grid inside damaged regions only.
	We can then start from the left-top corner of the visible rectangle,
	add the offset to get the coordinates of the first line, and start drawing from there.

TODO
*	Limit drawing the grid to the damaged regions only.