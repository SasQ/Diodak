*	Simplest scrollable window using `wxScrolledWindow` (an alias for `wxScrolled<wxPanel>`).
*	This time we set its scrollbars by Method 1: setting the scroll range indirectly by using scroll units.
	One scroll unit will be 10 pixels, which will later correspond to the distance between grid lines.
*	We also set the initial scroll position (in units).